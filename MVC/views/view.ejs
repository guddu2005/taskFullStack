<!-- <!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Task View</title>
   <style>
      body {
         font-family: Arial, sans-serif;
         background: linear-gradient(to right, #c4c9cb, #9fc6ef);
         color: #333;
         margin: 0;
         padding: 20px;
         animation: fadeIn 1s ease-in;
         display: flex;
         justify-content: center;
         align-items: center;
         height: 100vh;
         overflow: hidden;
         position: relative;
      }

      h2 {
         text-align: center;
         animation: slideIn 0.5s ease-in-out;
         color: #fff;
      }

      p {
         text-align: center;
         animation: slideIn 0.7s ease-in-out;
         color: #fff;
      }

      a,
      button {
         display: inline-block;
         margin: 10px auto;
         padding: 10px 20px;
         color: #fff;
         background-color: #ff6347;
         border: none;
         border-radius: 5px;
         text-decoration: none;
         text-align: center;
         transition: background-color 0.3s ease, transform 0.3s ease;
         cursor: pointer;
         animation: pulse 2s infinite;
      }

      a:hover,
      button:hover {
         background-color: #ff4500;
         transform: translateY(-3px);
      }

      form {
         text-align: center;
      }

      form button {
         background-color: #ff6347;
      }

      form button:hover {
         background-color: #ff4500;
      }

      .task-container {
         border: 1px solid rgba(255, 255, 255, 0.7);
         box-shadow: inset 5px 5px 8px rgba(255, 255, 255, 0.3);
         border-radius: 15px;
         width: 550px;
         padding: 20px;
         background: rgba(255, 255, 255, 0.1);
         animation: fadeIn 1.5s ease-in-out;
         transform: scale(0.9);
         transition: transform 0.5s ease-in-out;
      }

      .task-container:hover {
         transform: scale(1);
      }

      @keyframes fadeIn {
         from {
            opacity: 0;
         }

         to {
            opacity: 1;
         }
      }

      @keyframes slideIn {
         from {
            transform: translateY(-20px);
            opacity: 0;
         }
         to {
            transform: translateY(0);
            opacity: 1;
         }
      }

      @keyframes pulse {
         0% {
            transform: scale(1);
         }
         50% {
            transform: scale(1.05);
         }
         100% {
            transform: scale(1);
         }
      }

      .bubble {
         position: absolute;
         bottom: -50px;
         background: rgba(255, 255, 255, 0.7);
         border-radius: 50%;
         opacity: 0.6;
         animation: rise 8s infinite ease-in;
      }

      @keyframes rise {
         0% {
            transform: translateY(0);
            opacity: 0.7;
         }
         50% {
            opacity: 1;
         }
         100% {
            transform: translateY(-100vh);
            opacity: 0.2;
         }
      }

      .bubble:nth-child(1) {
         width: 40px;
         height: 40px;
         left: 20%;
         animation-duration: 5s;
      }

      .bubble:nth-child(2) {
         width: 20px;
         height: 20px;
         left: 40%;
         animation-duration: 6s;
      }

      .bubble:nth-child(3) {
         width: 50px;
         height: 50px;
         left: 60%;
         animation-duration: 7s;
      }

      .bubble:nth-child(4) {
         width: 25px;
         height: 25px;
         left: 80%;
         animation-duration: 8s;
      }

      .bubble:nth-child(5) {
         width: 30px;
         height: 30px;
         left: 50%;
         animation-duration: 9s;
      }
   </style>
</head>

<body>
   <div class="task-container" style="background-color: gray;">
      <h2>
         <%= task.title %>
      </h2>
      <p>
         <%= task.description %>
      </p>
      <div style="text-align: center;">
         <a href="/task/edit/<%= task._id %>">EDIT</a>
         <form action="/task/<%= task._id %>?_method=DELETE" method="POST" style="display: inline;">
            <button type="submit">Delete</button>
         </form>
         <a href="/task">Back to tasks</a>
      </div>
   </div>
   <div class="bubble"></div>
   <div class="bubble"></div>
   <div class="bubble"></div>
   <div class="bubble"></div>
   <div class="bubble"></div>
</body>

</html> -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task Details</title>
    <style>
        /* Apply general styling to the body */
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }

        /* Style the task container */
        
        .task-container {
            
            padding: 20px;
            margin: 20px auto;
            max-width: 600px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        /* Style the task title */
        .task-container h2 {
            margin-top: 0;
        }

        /* Style the task description */
        .task-container p {
            font-size: 16px;
        }

        /* Style the action buttons */
        .task-container a {
            color: #007bff;
            text-decoration: none;
            margin-right: 10px;
        }

        .task-container a:hover {
            text-decoration: underline;
        }

        .task-container button {
            background-color: #dc3545;
            border: none;
            color: white;
            padding: 10px 20px;
            font-size: 16px;
            border-radius: 4px;
            cursor: pointer;
        }

        .task-container button:hover {
            background-color: #c82333;
        }

        /* Style the bubbles */
        .bubble {
            width: 50px;
            height: 50px;
            background-color: rgba(0, 0, 0, 0.1);
            border-radius: 50%;
            position: absolute;
            bottom: 0;
            animation: bubble-animation 5s infinite;
        }

        .bubble:nth-child(1) {
            left: 10%;
            animation-delay: 0s;
        }

        .bubble:nth-child(2) {
            left: 30%;
            animation-delay: 1s;
        }

        .bubble:nth-child(3) {
            left: 50%;
            animation-delay: 2s;
        }

        .bubble:nth-child(4) {
            left: 70%;
            animation-delay: 3s;
        }

        .bubble:nth-child(5) {
            left: 90%;
            animation-delay: 4s;
        }

        @keyframes bubble-animation {
            0% {
                transform: translateY(0);
                opacity: 1;
            }
            100% {
                transform: translateY(-100vh);
                opacity: 0;
            }
        }
    </style>
</head>
<body>
    <div class="task-container">
        <h2>
            <%= task.title %>
        </h2>
        <p>
            <%= task.description %>
        </p>
        <div style="text-align: center; ">
           <button style="background-color: blue; ">
               <a href="/task/edit/<%= task._id %>" style="color:white;">EDIT</a>
            </button>
            <form action="/task/<%= task._id %>?_method=DELETE" method="POST" style="display: inline;">
                <button type="submit">Delete</button>
            </form>
            <button style="background-color: green;"><a href="/task" style="color:white;">Back to tasks</a></button>
        </div>
    </div>
    <div class="bubble"></div>
    <div class="bubble"></div>
    <div class="bubble"></div>
    <div class="bubble"></div>
    <div class="bubble"></div>
</body>
</html>
